import { FC } from 'react';

export const Header: FC = () => (
  <div className="App">
    <header className="App-header">
      <img src="./logo.svg" className="App-logo" alt="logo" />
      <h1 className="App-title">MyCato</h1>
    </header>
  </div>
);
